{
  "compilerOptions": {
    "target": "es2016",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible 
    // "rootDir": "../client/sayxiis/src",                                  /* Specify the root folder within your source files. */
    "outDir": "./dist",                                   /* Specify an output folder for all emitted files. */
    "esModuleInterop": true,                             /* Emit additional JavaScript to ease support for importing CommonJS modules. This enables 
    "strict": true,                                      /* Enable all strict type-checking options. */
    "skipLibCheck": true,
    "moduleResolution":"node16",                              /* Skip type checking all .d.ts files. */
    // "moduleResolution":"node",     
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true                         /* Skip type checking all .d.ts files. */
  }
}
